# 0.4.0 验证记录

2026-09-15，在 Windows x64、Electron 44.3.0、WebTorrent 3.0.21 和 FFmpeg 9.0.1 环境中验证。

## 引擎及接口

`npm test`：57 项通过、0 失败、0 跳过。

2026-09-17，在整理后的独立仓库目录再次执行：57 项通过、0 失败、0 跳过，约 23.35 秒。构建与测试截图输出路径已改为仓库内相对路径；本次整理未修改产品运行逻辑。

覆盖元数据解析、确认选择前零内容请求、文件子集、续传、私有种子发现限制、DHT 恢复、真实 TCP/HTTP Tracker、Range 请求、转码取消和播放调度。

17 项搜索测试覆盖本地 Torznab、配置加密保存、重启、分页、取消、旧响应隔离、来源错误隔离、哈希合并、原始磁力信息保留、受限重定向、XML/响应体限制、Archive 元数据解析及 Academic Torrents 缓存。

## 桌面测试

实际 Windows 打包程序通过 `test:ui`、`test:compat`、`test:fullscreen` 和 `test:search`。

- 原创 WebM 在 24 KiB/s 限速下，下载约 39.56% 时开始播放；未勾选的独立片段文件下载量为 0。
- 原创 MPEG4/AC3 MKV 在 256 KiB/s 限速下，下载约 8.61% 时已转码播放；跳转到 12 秒再返回 4 秒成功。
- 原生和自定义全屏按钮、Esc 返回与窗口尺寸恢复正常。
- 取消下载限速后引擎及诊断同步，原上传限速保留；部分设置更新、无效值拒绝正常。
- 搜索结果解析后仍需确认文件选择，未确认前内容下载为 0 字节。

上述是本地受控测试结果，不代表公网下载速度或任意视频的起播进度。

## 公网样本

在实际桌面程序中搜索 Sintel，取得 Internet Archive 30 条结果及官方开放影片示例 1 条。仅解析官方 CC BY 3.0 示例种子，哈希匹配、文件列表非空且全不选，未下载影片内容。

Academic Torrents 在该网络环境连接失败，界面显示单独的来源错误。网络可达性及返回数量可能变化。

## 复验

先按 README 安装依赖和 FFmpeg，再运行测试命令。测试使用独立临时数据目录。公网检查 `node scripts/check-public-search.mjs` 需要外网，不属于离线测试通过的依据。
